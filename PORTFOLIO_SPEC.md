One-column layout, max width 820px

Minimal UI: no animations, no gradients, no hero images

Routes: /professional (default) and /builder

Projects as text list (not card grid)

Add /projects list + 3 placeholder project pages

Must be fast: Lighthouse target 95+

Don’t add dependencies unless necessary